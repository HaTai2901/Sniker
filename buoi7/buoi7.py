"""
list : là một dãy danh sách chứa các dữ liệu có thể giống nhau,  ngoặc []
dict : gồm key : name
tuple : giống với list , ngoặc ()
set : giống list nhưng không trùng  lặp


len() : đếm số phần tử 
append() : thêm vô cuối list
insert() : thêm phần tử vô vị trí được cho , (vịtri, phần tử chèn)
extend() : kết hợp 2 list lại
remove() : loại bỏ phần tử theo tên trong list
pop() : loại bỏ phần tử nhưng theo index trong list

hàm if
if dieukien
    lệnh
elseif
    lệnh của else khi lệnh if không thỏa
else:
    lệnh cuối của hàm if khi các lệnh trên ko thỏa


    

% chia lấy dư
/ chia bình thường
// chia lấy nguyên
+= cộng rồi gán lên flag
-+ trừ rồi gán

ép kiểu khi input
a = int(input("sadasdasdsadsa"))

vòng lặp
for i(tự cho) in range(5): //nghĩa là cho vòng lặp chạy 5 lần
    lệnh của for

for num in range (1,5): lap dãy từ 1 đến 5 , in ra các số từ 1 đến 5

while true : là vòng lặp vô tận


"""


""" đếm pt trong list đếm
nếu cuối lớn hơn 0 , xóa lớn hơn 0, rôi rồitheem phần tử nhỏ cuối list"""


# num = list(range(1,6))

# for i in num:
#     if i == num[-1] and i > 0:
#         num.pop()
#         new_num = int(input("nhap so moi: "))
#         num.append(new_num)
# print(num)


